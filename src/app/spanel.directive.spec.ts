import { SpanelDirective } from './spanel.directive';

describe('SpanelDirective', () => {
  it('should create an instance', () => {
    const directive = new SpanelDirective();
    expect(directive).toBeTruthy();
  });
});
